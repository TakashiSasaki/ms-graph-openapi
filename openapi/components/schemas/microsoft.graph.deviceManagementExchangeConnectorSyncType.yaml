title: deviceManagementExchangeConnectorSyncType
enum:
  - fullSync
  - deltaSync
type: string
description: The type of Exchange Connector sync requested.
x-ms-enum:
  name: deviceManagementExchangeConnectorSyncType
  modelAsString: false
  values:
    - value: fullSync
      description: Discover all the device in Exchange.
      name: fullSync
    - value: deltaSync
      description: >-
        Discover only the device in Exchange which have updated during the delta
        sync window.
      name: deltaSync
